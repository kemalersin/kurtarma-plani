# Production (`npm run build`, CI) — kopyala: cp .env.production.example .env.production
# false = npm @senkronla/* tercih; paket yoksa ../senkronla veya SENKRONLA_ROOT fallback
VITE_LOCAL_SENKRONLA=false
VITE_ESR_RELAY_URL=https://senkron.la/api/v1
VITE_ESR_APP_ID=esr_app_kurtar_co
